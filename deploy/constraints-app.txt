# App-env pins, captured from the validated Mac environment (miniforge base,
# python 3.12) on 2026-07-19. Constraints only bite for packages the three
# pyprojects actually require; exojax==2.2.3 is already exact in
# vulcan-retrieval's pyproject and repeated here for clarity.
jax==0.9.2
jaxlib==0.9.2
exojax==2.2.3
numpy==2.0.2
scipy==1.17.0
matplotlib==3.10.0
pandas==2.2.3
astropy==7.2.0
streamlit==1.59.1
